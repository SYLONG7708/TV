name: Check OKTV public freshness

on:
  workflow_dispatch:
  schedule:
    - cron: "37 */3 * * *"

permissions:
  actions: write
  contents: read

jobs:
  check:
    uses: ./.github/workflows/oktv-self-heal.yml
